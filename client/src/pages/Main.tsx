import { HeroHeader, HomeView } from '../components';

const MainPage = () => {
  return (
    <div>
      <HeroHeader type="user-view" />
      <HomeView />
    </div>
  );
};

export default MainPage;
